# Note: `record_audio` is a local function in voice/recorder.py, not a pip package.
fastapi==0.115.6
uvicorn[standard]==0.32.1
openai-whisper>=20231117
sounddevice==0.5.1
numpy>=1.26.0,<3.0
webrtcvad==2.0.10
pydantic==2.6.4
python-dotenv==1.0.1
requests==2.31.0
sentence-transformers>=2.7.0
pyarrow>=17.0.0
pytest>=8.0
mediapipe>=0.10.0
opencv-python>=4.8.0.0
pyglet>=2.0.0
Pillow>=10.0.0
PyOpenGL>=3.1.7

# LLM clients (pipeline/, llm/) — google-genai talks to Gemini via Vertex AI (ADC,
# no API key), groq/openai are available but pipeline code currently calls Groq's
# REST endpoint directly via `requests` rather than the groq SDK.
google-genai>=1.75.0
groq>=1.2.0
openai>=2.30.0

# WebSocket support for backend/api_server.py's /ws/pipeline route. Also pulled in
# transitively by uvicorn[standard], pinned explicitly since pipeline code depends
# on it directly now.
websockets>=15.0.1

# Redis cache (pipeline/cache.py) — hard top-level import, must always be
# installed even though the system degrades gracefully if the Redis *server*
# is unreachable at runtime.
redis>=4.5.5
